#include "prelude.lisp"
#include "Union.lisp"

(defun (read) () (Char (read_)))
(defun (= String Char) (s c) (if (&& (= (car (:list S) (c))))
(defun readall_impl (char) (if char (cons char (readall_impl (read))) (nil (Char 0) )))

(defun readall () (readall_impl (read)))

(defun || (a b) (if a 1 b))
(defun || (a b c) (if a 1 (if b 1 c)))
  
(defun continue_parse_word (char)
  (if (|| (= char 10) (= char 32) (= char 41))


(defun main () 
  (do 
    (print (readall))
    (print (St "asdf" (None)))
    0
    )
  )
